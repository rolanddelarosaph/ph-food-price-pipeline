name: 'ph_food_pipeline'
version: '1.0.0'
config-version: 2

profile: 'ph_food_pipeline'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  ph_food_pipeline:
    bronze:
      +materialized: table
      +schema: bronze
      +database: ph_food_pipeline
    silver:
      +materialized: table
      +schema: silver
      +database: ph_food_pipeline
    gold:
      +materialized: table
      +schema: gold
      +database: ph_food_pipeline